let score ="43";
console.log(typeof score );  // is type is numbar but when we change it 

let valueNumber = Number (score) // we convrt it into number 
console.log(typeof valueNumber);  // now it type of number 


let islogin = 1
 let boolcon = Boolean(islogin)
 console.log(typeof boolcon);
 
